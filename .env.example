# Copy to .env and fill in real values. Never commit .env.

# Required: OpenAI API key for ChatOpenAI
OPENAI_API_KEY=

# Optional: identifies HTTP clients (e.g. DuckDuckGo search). Reduces warnings.
USER_AGENT=ProjectNexus/1.0 (dev)
